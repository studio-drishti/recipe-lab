type Modification {
  id: ID! @id
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  user: User!
  recipe: Recipe!
  sortings: [Sorting!]!
  alterations: [Alteration!]!
  removals: [ID!]!
  itemAdditions: [ItemAddition!]!
  stepAdditions: [StepAddition!]!
  ingredientAdditions: [IngredientAddition!]!
}

type Sorting @embedded {
  uid: ID! @id
  parentId: ID!
  order: [ID!]!
}

type Alteration @embedded {
  uid: ID! @id
  sourceId: ID!
  field: String!
  value: String!
}

type ItemAddition @embedded {
  uid: ID! @id
  clientId: ID!
  parentId: ID!
  name: String!
}

type StepAddition @embedded {
  uid: ID! @id
  id: ID!
  parentId: ID!
  directions: String!
  notes: String!
}

type IngredientAddition @embedded {
  uid: ID! @id
  id: ID!
  parentId: ID!
  name: String!
  quantity: String!
  unit: String
  processing: String
}
