#import './ModificationFragment.graphql'
query recipe($uid: ID!, $user: ID) {
  recipe(uid: $uid) {
    uid
    title
    description
    servingAmount
    servingType
    time
    items {
      uid
      name
      steps {
        uid
        directions
        notes
        ingredients {
          uid
          name
          quantity
          unit
          processing
        }
      }
    }
    photos {
      id
      filename
      url
    }
    author {
      name
      avatar
    }
    modification(user: $user) {
      ...ModificationInfo
    }
  }
}
