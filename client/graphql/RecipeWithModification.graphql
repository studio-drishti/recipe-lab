#import './ModificationFragment.graphql'
query recipe($uid: ID!, $user: ID) {
  recipe(uid: $uid) {
    uid
    title
    description
    course
    time
    skill
    items {
      uid
      name
      steps {
        uid
        directions
        notes
        ingredients {
          uid
          name
          quantity
          unit
          processing
        }
      }
    }
    author {
      name
      avatar
    }
    modification(user: $user) {
      ...ModificationInfo
    }
  }
}
