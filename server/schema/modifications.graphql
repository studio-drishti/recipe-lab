type Modification {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  user: User!
  recipe: Recipe!
  sortings: [Sorting!]!
  alterations: [Alteration!]!
  removals: [ID!]!
  additions: [Addition!]!
  itemAdditions: [ItemAddition!]!
  stepAdditions: [StepAddition!]!
  ingredientAdditions: [IngredientAddition!]!
}

type Sorting {
  uid: ID!
  parentId: ID!
  order: [ID!]!
}

input SortingInput {
  uid: ID
  parentId: ID!
  order: [ID!]!
}

type Alteration {
  uid: ID!
  sourceId: ID!
  field: String!
  value: String!
}

input AlterationInput {
  uid: ID
  sourceId: ID!
  field: String!
  value: String!
}

union Addition = ItemAddition | StepAddition | IngredientAddition

type ItemAddition {
  id: ID!
  name: String!
}

input ItemAdditionInput {
  id: ID!
  name: String!
}

type StepAddition {
  id: ID!
  directions: String!
  notes: String!
}

type IngredientAddition {
  id: ID!
  name: String!
  quantity: Float!
  unit: String
  processing: String
}
